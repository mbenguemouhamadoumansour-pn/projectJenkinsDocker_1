pipeline {
    agent any

    environment {
        DOCKERHUB_USERNAME = 'mouhamadoumbengue'
        DOCKERHUB_CREDENTIALS = credentials('dockerhub-credentials')
        FRONTEND_IMAGE = "${DOCKERHUB_USERNAME}/sama-frontend"
        BACKEND_IMAGE = "${DOCKERHUB_USERNAME}/sama-backend"
        BUILD_TAG = "${BUILD_NUMBER}"
        NOTIFICATION_EMAIL = 'mbenguemouhamadoumansour@gmail.com'
        EC2_HOST = '35.92.219.49'
        EC2_USER = 'ubuntu'
    }

    stages {
        stage('Checkout') {
            steps {
                echo '📦 Récupération du code depuis GitHub...'
                checkout scm
            }
        }

        stage('Install Dependencies') {
            parallel {
                stage('Backend Dependencies') {
                    steps {
                        dir('backend') {
                            echo '📦 Installation des dépendances backend...'
                            sh 'npm install'
                        }
                    }
                }
                stage('Frontend Dependencies') {
                    steps {
                        dir('frontend') {
                            echo '📦 Installation des dépendances frontend...'
                            sh 'npm install'
                        }
                    }
                }
            }
        }

        stage('Run Tests') {
            parallel {
                stage('Test Backend') {
                    steps {
                        dir('backend') {
                            echo '🧪 Tests backend...'
                            sh 'npm test || echo "⚠️ Aucun test configuré pour le backend"'
                        }
                    }
                }
                stage('Test Frontend') {
                    steps {
                        dir('frontend') {
                            echo '🧪 Tests frontend...'
                            sh 'CI=true npm test || echo "⚠️ Aucun test configuré pour le frontend"'
                        }
                    }
                }
            }
        }

        stage('Build Docker Images') {
            parallel {
                stage('Build Backend Image') {
                    steps {
                        dir('backend') {
                            echo "🐳 Construction de l'image backend..."
                            sh "docker build -t ${BACKEND_IMAGE}:${BUILD_TAG} ."
                            sh "docker tag ${BACKEND_IMAGE}:${BUILD_TAG} ${BACKEND_IMAGE}:latest"
                        }
                    }
                }
                stage('Build Frontend Image') {
                    steps {
                        dir('frontend') {
                            echo "🐳 Construction de l'image frontend..."
                            sh "docker build -t ${FRONTEND_IMAGE}:${BUILD_TAG} ."
                            sh "docker tag ${FRONTEND_IMAGE}:${BUILD_TAG} ${FRONTEND_IMAGE}:latest"
                        }
                    }
                }
            }
        }

        stage('Push to DockerHub') {
            steps {
                echo '🔑 Connexion à DockerHub...'
                sh 'echo $DOCKERHUB_CREDENTIALS_PSW | docker login -u $DOCKERHUB_CREDENTIALS_USR --password-stdin'

                echo '📤 Push des images vers DockerHub...'
                sh "docker push ${BACKEND_IMAGE}:${BUILD_TAG}"
                sh "docker push ${BACKEND_IMAGE}:latest"
                sh "docker push ${FRONTEND_IMAGE}:${BUILD_TAG}"
                sh "docker push ${FRONTEND_IMAGE}:latest"

                echo '✅ Images publiées sur DockerHub avec succès!'
            }
        }

        stage('Deploy to EC2') {
            steps {
                echo '🚀 Déploiement sur AWS EC2...'

                sshagent(credentials: ['ec2-deploy-key']) {
                    sh """
                        ssh -o StrictHostKeyChecking=no ${EC2_USER}@${EC2_HOST} \\
                        "echo \\"🔍 Vérification de Docker...\\" && \\
                        if ! command -v docker &> /dev/null; then \\
                            echo \\"❌ Docker n'est pas installé sur EC2\\" && exit 1; \\
                        fi && \\
                        echo \\"🛑 Arrêt des anciens conteneurs...\\" && \\
                        docker stop sama-backend || true && \\
                        docker rm sama-backend || true && \\
                        docker stop sama-frontend || true && \\
                        docker rm sama-frontend || true && \\
                        echo \\"📥 Pull des images...\\" && \\
                        docker pull ${BACKEND_IMAGE}:latest && \\
                        docker pull ${FRONTEND_IMAGE}:latest && \\
                        echo \\"🚀 Démarrage du backend...\\" && \\
                        docker run -d --name sama-backend -p 5000:5000 ${BACKEND_IMAGE}:latest && \\
                        echo \\"🚀 Démarrage du frontend...\\" && \\
                        docker run -d --name sama-frontend -p 3000:3000 ${FRONTEND_IMAGE}:latest && \\
                        echo \\"✅ Déploiement terminé sur EC2\\"
                        "
                    """
                }

                echo '✅ Déploiement sur EC2 terminé!'
            }
        }
    }

    post {
        success {
            echo '🎉 Pipeline terminé avec succès!'
            emailext (
                subject: "✅ Build #${BUILD_NUMBER} - DÉPLOYÉ AVEC SUCCÈS - ${JOB_NAME}",
                body: """
                    <html>
                    <body>
                        <h2 style="color: green;">✅ Application déployée avec succès sur EC2 !</h2>
                        <ul>
                            <li><strong>Projet:</strong> ${JOB_NAME}</li>
                            <li><strong>Build:</strong> #${BUILD_NUMBER}</li>
                            <li><strong>Date:</strong> ${new Date()}</li>
                            <li><a href="http://${EC2_HOST}:3000">Frontend</a></li>
                            <li><a href="http://${EC2_HOST}:5000">Backend</a></li>
                        </ul>
                    </body>
                    </html>
                """,
                to: "${NOTIFICATION_EMAIL}",
                mimeType: 'text/html'
            )
        }

        failure {
            echo '❌ Le pipeline a échoué!'
            emailext (
                subject: "❌ Build #${BUILD_NUMBER} - ÉCHEC DÉPLOIEMENT - ${JOB_NAME}",
                body: """
                    <html>
                    <body>
                        <h2 style="color: red;">❌ Le déploiement sur EC2 a échoué</h2>
                        <p><a href="${BUILD_URL}console">📋 Voir les logs Jenkins</a></p>
                    </body>
                    </html>
                """,
                to: "${NOTIFICATION_EMAIL}",
                mimeType: 'text/html'
            )
        }

        always {
            echo '🧹 Nettoyage des images locales...'
            sh """
                docker rmi ${BACKEND_IMAGE}:${BUILD_TAG} || true
                docker rmi ${BACKEND_IMAGE}:latest || true
                docker rmi ${FRONTEND_IMAGE}:${BUILD_TAG} || true
                docker rmi ${FRONTEND_IMAGE}:latest || true
            """
            sh 'docker logout || true'
        }
    }
}
